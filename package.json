{
  "name": "twilio-webhook-proxy",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "start": "twilio-run",
    "deploy": "twilio-run deploy --env .env.staging -n twilio-webhook-proxy-staging",
    "deploy-prod": "twilio-run deploy --environment=prod -n twilio-webhook-proxy-production --env .env.production"
  },
  "dependencies": {
    "@sentry/node": "^7.43.0",
    "@sentry/tracing": "^7.43.0",
    "@twilio/runtime-handler": "1.3.0",
    "axios": "^1.3.4",
    "fast-xml-parser": "^4.1.3",
    "lodash": "^4.17.21",
    "twilio": "^4.8",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "axios-mock-adapter": "^1.21.2",
    "jest": "^29.5.0",
    "twilio-run": "^3.5.3"
  },
  "engines": {
    "node": "14"
  }
}
